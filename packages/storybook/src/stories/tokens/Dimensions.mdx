import { Meta } from '@storybook/blocks';
import { SizingTokens } from '../../components/TokenStatus';

<Meta title="Fondations/Dimensions" />

# Dimensions

Les tokens de dimensions définissent les tailles fixes applicables aux composants et aux éléments (hauteurs, largeurs, icônes…).
La largeur de chaque barre est proportionnelle à la valeur du token.

<SizingTokens />
